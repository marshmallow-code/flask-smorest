[tox]
envlist = py34, py35, py36

[testenv]
deps =
    flake8>=3.0.0
    pytest>=2.8
    pytest-cov>=2.4.0
    paginate>=0.5.5
commands =
    flake8 .
    pip install -e .
    py.test --cov flask_rest_api
